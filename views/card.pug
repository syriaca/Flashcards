extends layout.pug

block content
    h2= text
    if hint 
        p
            i Hint: #{hint}
        a(href=`${id}?side=${sideToShow}`) #{sideToShow}
        br
        a(href=`/cards`) Next card


